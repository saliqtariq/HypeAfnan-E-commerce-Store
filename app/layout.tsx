// Root layout — the [locale] layout handles all rendering.
// This file is required by Next.js but defers to the locale layout.
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
